# la sintassi ${MYSQL_HOST:localhost} significa che se la variabile di ambiente MYSQL_HOST e' definita 
# si usera' quella, altrimenti verra' utilizzato il valore di fallback localhost.

spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/aips
spring.datasource.username=pippo
spring.datasource.password=Merda1
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql: true

